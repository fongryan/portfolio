# Per-repo no-mistakes overrides for the portfolio hub.
#
# The repo-native proof is authoritative locally, in CI, and in Vercel.

commands:
  test: "npm run proof"

# Keep test evidence out of this public repo; no-mistakes stores it in a temp dir.
test:
  evidence:
    store_in_repo: false
