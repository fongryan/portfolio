# Buildout skill

Before changing a workspace, read the composition draft, verify every module
digest, and emit a plan ordered by dependencies. Run focused tests after each
module and return a compact proof receipt. Never install a module that is not
present in the approved draft.
